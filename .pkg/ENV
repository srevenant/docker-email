_name=docker-email
_org=revenant
# NO_DOCKER_CACHE if defined will add --no-cache to docker build
# BUILD_VERSION, if defined, will add it as a tag to the build image
export DOCKER_IMAGE=${_org}/${_name}
export DOCKER_NAME=${_name}
export DOCKER_TAGS=    # a list of tags to add to the build
